@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Fmt
if(@ILIAS_USE_FMT@)
    find_dependency(fmt)
endif()

# Spdlog
if(@ILIAS_USE_SPDLOG@)
    find_dependency(spdlog)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/IliasTargets.cmake")

check_required_components(ilias)