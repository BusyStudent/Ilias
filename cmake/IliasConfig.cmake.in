@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# 如果构建时用到了 fmt，使用者也需要 fmt
if(@ILIAS_USE_FMT@)
    find_dependency(fmt)
endif()

# 如果构建时用到了 spdlog
if(@ILIAS_USE_SPDLOG@)
    find_dependency(spdlog)
endif()

# 恢复 Target 定义
include("${CMAKE_CURRENT_LIST_DIR}/IliasTargets.cmake")

check_required_components(ilias)